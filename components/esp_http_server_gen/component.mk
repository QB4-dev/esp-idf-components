#component Makefile
COMPONENT_ADD_INCLUDEDIRS = include

SERVER_DATA_DIR    = $(PROJECT_PATH)/srv
GENERATED_CODE_DIR = $(PROJECT_PATH)/generated

GENERATWED := $(shell $(COMPONENT_PATH)/gen_code.sh $(SERVER_DATA_DIR) $(GENERATED_CODE_DIR))
COMPONENT_EMBED_FILES := $(shell [ -d $(SERVER_DATA_DIR) ] && find $(SERVER_DATA_DIR) -type f)

#must be relative to COMPONENT_PATH
COMPONENT_SRCDIRS := . 
COMPONENT_SRCDIRS += $(shell realpath --relative-to="$(COMPONENT_PATH)" $(GENERATED_CODE_DIR) )

COMPONENT_OWNCLEANTARGET := 1

clean:
	rm -rf $(GENERATED_CODE_DIR)
